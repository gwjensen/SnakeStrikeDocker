#!/bin/bash
set -e

#Have to set the owner and group on these files so that things can be built in these directories
#chown -R "${HOST_UID}:${HOST_GID}" /opt/hostmot2-firmware
#chown -R "${HOST_UID}:${HOST_GID}" /opt/Xilinx/
